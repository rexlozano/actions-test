name: Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Print ${{ env.BLAH }}
        env:
          BLAH: blah
        run: echo "Print ${{ env.BLAH }}"
