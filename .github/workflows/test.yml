name: Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Save
        run: echo "BLAH=${{ secrets.BLAH }}" >> $GITHUB_ENV
      - name: Print
        run: echo "Print ${{ env.BLAH }}"
